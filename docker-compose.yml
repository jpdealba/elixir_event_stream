services:
  sqs:
    image: softwaremill/elasticmq-native
    ports:
      - "9324:9324"

  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: alert_media_dev
